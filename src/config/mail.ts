interface IMailConfig {
    driver: 'ethereal' | 'ses';

    defaults: {
        from: {
            email: string;
            name: string;
        };
    };
}

export default {
    driver: process.env.MAIL_DRIVER || 'ethereal',

    defaults: {
        from: {
            email: 'bruno@psmanager.com.br',
            name:  'Bruno do Personal Manager',
        },
    },
} as IMailConfig;